version: "3"

services:
  bitwarden:
        container_name: "bitwarden"
        image: "vaultwarden/server:1.22.2-alpine"
        restart: always
        env_file:
          - ".env"
        volumes:
          - "./bw_data:/data"
        ports:
          - "8080:80"
